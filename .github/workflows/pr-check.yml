name: pull request check

on:
  pull_request:
    branches: 
        - main
    types: [opened, synchronize]

jobs:
  pr-check:
    runs-on: ubuntu-latest

    steps:
      - name: PR check running for branch
        run: echo "PR check is running for branch ${{ github.head_ref }}"